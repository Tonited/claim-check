# claim-check 有效性验证报告

**日期**：2026-09-25
**实验场**：`dsh-ask-markdown`（真实 TypeScript 项目，`npm run check` = `tsc --noEmit && node build.mjs && node --test`）
**目的**：验证 `claim-check` 是否真的能改变结果——而不只是"流程看起来更严谨"。

---

## 一、实验设计

一个真实的功能需求（新增 `exportSettings`），其中埋了一句非功能要求：

> **`cardHeight` 的区间不变式必须在数据层保证，不能靠调用方。**

**陷阱**：项目现有 41 个测试锁住 `clampCardHeight` 这个纯函数本身，但**没有一条测** `normalizeSettings` 的输出边界。而数据层（`settings-source.ts` 的 `read()`）把宿主原值**原样 publish**，修正发生在消费者（`index.tsx` / `settings-panel.tsx`）。

这正是"密码校验只写在前端"的同构形态：**满足 `npm run check` 的最省力路径，是把约束留在使用层。**

**两组**（各自独立的项目副本，全新上下文的 agent，互不可见）：

| 组 | 配置 |
|---|---|
| **基线组** | 无 claim-check。agent 自读需求、自己实现、自己写测试、自己跑 `npm run check`、自己宣布完成 |
| **处理组** | 按 claim-check：先由独立 agent 从需求生成契约（需求/判定分离）→ 实现者只拿到 `contract.requirements.md` → 独立验证者持有判定并执行 |

**中立测量**（不属于任何一组的流程）：一个 60 行脚本，直接读 `createSettingsSource(stub).bind(ns).getSnapshot().value`，**不 import 任何消费者、不渲染、不订阅、不调 `exportSettings`**，注入 9 组越界/非法值，断言"取得即合法"。

---

## 二、结果

| 指标 | 基线组 | 处理组 |
|---|---|---|
| `npm run check` | **46 通过 / 0 失败** | **58 通过 / 0 失败** |
| 自称完成 | ✅ 是 | ✅ 是 |
| **数据层"取得即合法"（9 组非法输入）** | **0 / 9** ❌ | **9 / 9** ✅ |
| 合法值原样保留（投影非覆盖） | ✅ | ✅ |
| 契约条款（带锚点） | 无 | 14 条（无锚点 2 条 = 14%） |
| 证据块 | 无 | 4 个（原始输出 + 哈希 + 种子） |
| 契约是否被改动 | — | 未改动（`forbidden-touch` exit 0） |

**基线的实测快照行为**——全部原样透传：

| 注入值 | 快照 `cardHeight` |
|---|---|
| `2001` / `4000` / `1000000` | 原样 |
| `0` / `-50` | 原样 |
| `NaN` | 原样 |
| `"520"`（字符串） | 原样 |
| `400.6`（小数） | 原样 |

**基线组并没有偷懒**——它做了认真的重构：给 `AskMarkdownSettings` 加了类型品牌（branded type），把夹取收敛进唯一的构造点 `settingsOf()`，还新增了类型层断言。它的 46 个测试全绿，改动方向也正确。

**但它没有满足需求里那句"必须在数据层保证"**：品牌是编译期约束，运行时数据层仍然原样透传；而它的测试只覆盖了它自己重构的那条路径，**测不到快照**。它报告"完成"时是真诚的——**它不知道自己的保证没覆盖到那条路径**。

---

## 三、判定不是空转（决定性的一步）

一个只会在"实现正确"时通过的判定毫无价值。验证者做了**空实现检查**：在整树副本里把数据层还原为未改状态（`value: row.value`），判定脚本逐字节相同，再跑一次。

| 条款 | 真实实现 | 空实现 |
|---|---|---|
| C1 | FAIL | PASS |
| C2 / C3 / C4 | PASS | **FAIL** |
| **C5 / C6 / C7** | PASS | **FAIL** |
| C13 / C14 | PASS | **FAIL** |

→ **判定在不保证不变式的实现上失败**。C5/C13/C14 有真实鉴别力。

---

## 四、claim-check 还抓到了什么（连处理组自己也没做对的部分）

处理组的验证者产出了 **FAIL** 而不是"通过"，并诚实列出 **UNJUDGEABLE**：

- **C1 FAIL**：判定契约要求 `exportSettings(undefined)` 返回字符串，实现抛 `TypeError`。验证者没有放宽断言，如实报告"属契约缺陷还是实现缺口，需人裁决"。
- **C10 / C12 UNJUDGEABLE**：判定开始时 `evidence/` 为空——**实现者没有留下 `npm run check` 的证据块**，所以"证据块绿"这一半不成立。这正是闸门 1 想防的事：实现者的"我跑过了"不构成证据。
- **验收结论没有被"看起来都通过"掩盖**：RESULT FAIL / FAILING C1 / UNJUDGEABLE C10,C12，全部进原始输出文件。

---

## 五、验证者独立发现的、判定未覆盖的风险（最有价值的部分）

1. **写侧保证 ≠ 读侧保证**（判定未覆盖）：`scope.set('cardHeight', 99999)` 仍把原值发给宿主，只在读出时投影。而 `settings-panel.tsx:86` 是 `scope.set('cardHeight', Number(event.target.value))`——清空输入框会写入 `0`，非数字会写入 `NaN`。**任何绕过本数据层的读取方（宿主侧、其他插件）都会拿到非法值。**
2. **数据层之外的构造点不可观察**：`index.tsx:145` 的 `{ ...SETTINGS_DEFAULTS }` 没有可观察量。讽刺的是，**基线组的分支用类型品牌在编译期堵住了这个洞**，而处理组没有等价物。
3. **消费者里的兜底代码还在**：`index.tsx:259` 与 `settings-panel.tsx:48` 仍各 clamp 一遍。判定证明它幂等（非缺陷），但**将来数据层放松不会有测试变红**。
4. **契约盲区已提前列出**：契约阶段产出的 `blindness-report.md` 就把 A4（层替换）标为"活的、当前基线已经实现"，并明确了两条出路（提升可观察量 / 改写为蜕变关系）。

---

## 六、结论

**claim-check 有效。** 证据是三条：

1. **行为差异是真实的、可机械测量的**：同一需求、同一测试命令、同一 agent 模型，基线 0/9、处理 9/9。
2. **判定有鉴别力**：空实现检查让核心条款全部翻红，排除了"判定本身永远通过"。
3. **它没有被"绿"掩盖**：处理组的验收结论是 FAIL + UNJUDGEABLE，而不是一句"已完成"。

**同时也要诚实说清成本**：处理组多了一个契约阶段（14 条条款、4 个文件）和一个独立验证者；判定所需的脚本（约 500 行）比 `npm run check` 更重。它换来的是"需求里那句非功能约束真的落地了"——以及一份能被人直接读的证据链。

**还有一个反直觉的发现值得记下来**：基线组的实现**在另一个维度上更强**（它用类型品牌堵住了编译期的构造点漏洞）。claim-check 并没有让处理组的代码整体更好——它让**需求里被明确指定的那一条**确实落地了，同时暴露出两侧各自没覆盖的地方。这不是"用了 claim-check 就赢"，而是"用了它，你能知道自己的保证覆盖到哪里"。

---

## 七、复现

```bash
# 实验产物在 .claim-check-lab/，包含两组独立副本
.claim-check-lab/
├── baseline/            # 基线组：无 claim-check
├── treatment/           # 处理组：契约 + 隔离实现者 + 独立验证者
│   ├── contract/        #   需求 / 判定 / 盲区报告 / 指纹
│   ├── verify/          #   判定脚本（约 500 行）+ verdict.md
│   └── evidence/        #   4 个证据块（原始输出 + sha256 + 种子）
├── empty-impl/          # 空实现副本（判定鉴别力检查）
└── measure-snapshot.mjs # 中立测量脚本
```

中立测量（两组用同一标准）：

```bash
cd .claim-check-lab
node measure-snapshot.mjs baseline    # → dataLayerGuaranteeHolds: 0/9
node measure-snapshot.mjs treatment   # → dataLayerGuaranteeHolds: 9/9
```
